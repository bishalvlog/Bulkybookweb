@model IEnumerable<BulkyBook.Models.CoverType>
@{
    ViewData["Title"] = "CoverTypeList";
}
<partial name="Notification" /> 
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">CoverType List </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create new CoverType
            </a>
        </div>
        test
    </div>
</div>
<form>
    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Category
                </th>
                <th>
                    DisplayOrder
                </th>
                
            </tr>
        </thead>
        <tbody>
            @foreach (var i in Model)
            {
                <tr>
                    <td width="50%">
                        @i.Name
                    </td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="CoverType" asp-action="Edit" asp-route-id="@i.Id" class="btn btn-primary">
                                <i class="bi bi-pencil"></i>
                                Edit
                            </a>
                            <a asp-controller="CoverType" asp-action="Delete" asp-route-id="@i.Id" class="btn btn-danger">
                                <i class="bi bi-trash3"></i>
                                Delete
                            </a>

                        </div>
                    </td>
                 
                </tr>
            }
        </tbody>
    </table>
</form>



